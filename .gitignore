# mpv portable config

# Binaries (managed by updater.bat)
mpv.exe
mpv.com
fonts.conf

# Auto-generated cache (shader cache, watch_later)
portable_config/cache/

# Auto-generated logs/state (do not commit)
portable_config/tools/update-log.txt
portable_config/tools/update-state.json
portable_config/track-selector-overrides.json

# OS generated
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Local verification clips/logs (generated by tests/*.ps1, never commit)
tests/clips/
tests/*.log

# mpv build droppings re-deposited by Update-MpvEnvironment's mpv-extract step
# (robocopy /E over root): stale upstream docs + legacy installer, not source.
doc/
installer/
